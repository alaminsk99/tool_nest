import 'package:flutter/material.dart';

class ToolsPages extends StatelessWidget {
  const ToolsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("ToolsPages"),),
    );
  }
}
